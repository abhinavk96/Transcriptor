export default {
  name: 'ajax-service',
  initialize() {
    // noop
    // This is to override Fastboot's initializer which prevents ember-fetch from working
    // https://github.com/ember-fastboot/ember-cli-fastboot/blob/master/fastboot/initializers/ajax.js
  }
};




recID > session 1 > b1.WAV, Cei1.wav, close1.WAV
        session 2 > b2.WAV, ceil2.wav

meta.json

API CALL

output > recid > session1 > boundary > 1.2.stm, 1.2.ctm
                            closetalk > stm. ctm



Categorise by session
